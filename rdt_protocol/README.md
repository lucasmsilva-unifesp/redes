# redes

## Compilando e executando o programa
make clean - Limpa os arquivos gerados pela compilação

make - Compila o programa

make run-server PORT=8080 - Executa o servidor na porta 8080
make run-client IP=127.0.0.1 PORT=8080 - Executa o cliente conectando no IP